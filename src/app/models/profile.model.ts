export interface Profile {
  firstName: string,
  age: number,
  gender: string,
  manChecked: boolean,
  womanChecked: boolean,
  otherChecked: boolean,
  minAge: number,
  maxAge: number,
  description: string
}
